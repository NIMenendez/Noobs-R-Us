---

---

<div
  class="py-16 md:py-24 px-6 md:px-12 lg:px-24 bg-linear-to-b from-gray-900 to-gray-800"
>
  <div class="max-w-7xl mx-auto">
    <h2
      class="text-4xl md:text-5xl lg:text-6xl font-bold mb-12 text-center text-white"
    >
      Our Mission
    </h2>
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <!-- Image Carousel -->
      <div
        id="mission-carousel"
        class="relative w-full h-[400px] md:h-[500px] bg-gray-700 rounded-lg overflow-hidden"
      >
        <div class="carousel w-full h-full">
          <!-- Carousel slides -->
          <div
            class="carousel-item w-full h-full transition-opacity duration-500"
            data-slide="0"
          >
            <img
              src="/src/assets/756nords.png"
              alt="Racing at Nords"
              class="w-full h-full object-cover"
            />
          </div>
          <div
            class="carousel-item w-full h-full hidden transition-opacity duration-500"
            data-slide="1"
          >
            <img
              src="/src/assets/carsfrombehind.jpg"
              alt="Cars from behind"
              class="w-full h-full object-cover"
            />
          </div>
          <div
            class="carousel-item w-full h-full hidden transition-opacity duration-500"
            data-slide="2"
          >
            <img
              src="/src/assets/cotabattle.jpg"
              alt="COTA Battle"
              class="w-full h-full object-cover"
            />
          </div>
        </div>
        <!-- Carousel navigation dots -->
        <div
          class="absolute bottom-4 left-1/2 transform -translate-x-1/2 flex gap-2 z-10"
        >
          <button
            class="carousel-dot w-3 h-3 rounded-full bg-white opacity-75 hover:opacity-100 transition-opacity"
            data-slide="0"></button>
          <button
            class="carousel-dot w-3 h-3 rounded-full bg-white opacity-50 hover:opacity-75 transition-opacity"
            data-slide="1"></button>
          <button
            class="carousel-dot w-3 h-3 rounded-full bg-white opacity-50 hover:opacity-75 transition-opacity"
            data-slide="2"></button>
        </div>
      </div>

      <!-- Text Content -->
      <div class="space-y-6 max-w-2xl lg:ml-auto">
        <p
          class="text-lg md:text-xl lg:text-2xl text-right text-gray-300 leading-relaxed"
        >
          At Noobs "R" Us Racing, our mission is to provide an inclusive and
          supportive environment for sim-racers of all skill levels. We are
          dedicated to fostering a positive atmosphere where noob or pro,
          everyone feels welcome.
        </p>
        <p
          class="text-lg md:text-xl lg:text-2xl text-right text-gray-300 leading-relaxed"
        >
          We believe that everyone deserves the chance to experience the thrill
          of racing, regardless of their experience or background.
        </p>
        <p
          class="text-lg md:text-xl lg:text-2xl text-right text-gray-300 leading-relaxed"
        >
          Our community is built on the principles of camaraderie, learning, and
          fun, where members can grow their skills, participate in exciting
          events, and forge lasting friendships.
        </p>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const carousel = document.getElementById("mission-carousel");
    if (!carousel) return;

    const slides = carousel.querySelectorAll(".carousel-item");
    const dots = carousel.querySelectorAll(".carousel-dot");

    function showSlide(index: number) {
      // Hide all slides
      slides.forEach((slide) => {
        slide.classList.add("hidden");
      });

      // Show current slide
      slides[index].classList.remove("hidden");

      // Update dots
      dots.forEach((dot, i) => {
        if (i === index) {
          dot.classList.remove("opacity-50");
          dot.classList.add("opacity-75");
        } else {
          dot.classList.remove("opacity-75");
          dot.classList.add("opacity-50");
        }
      });
    }

    // Add click handlers to dots
    dots.forEach((dot, index) => {
      dot.addEventListener("click", () => {
        showSlide(index);
      });
    });
  });
</script>
